import 'package:flutter/material.dart';

class ErrorMax extends StatelessWidget {
  const ErrorMax({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ERROR'),
    );
  }
}
